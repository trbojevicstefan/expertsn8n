import { requireAdmin } from "@/lib/auth/server";import { StatusBadge } from "@/components/status-badge";import { experts } from "@/lib/demo-data";
export default async function AdminExperts(){await requireAdmin();return <><div className="portal-head"><div><h1>Expert verification</h1><p>Review identity consistency, CV evidence, profile content and workflow showcases.</p></div></div><div className="data-card card"><table className="data-table"><thead><tr><th>Expert</th><th>Profile</th><th>CV</th><th>Showcase</th><th>State</th><th></th></tr></thead><tbody>{experts.map((e,i)=><tr key={e.id}><td><div className="table-person"><img src={e.photoUrl} alt=""/><div><strong>{e.name}</strong><br/><span className="muted">{e.location}</span></div></div></td><td>Complete</td><td>{i===2?"Needs review":"Uploaded"}</td><td>{i===1?"1 pending":"Approved"}</td><td><StatusBadge tone={i===2?"warning":"success"}>{i===2?"UNDER_REVIEW":"PUBLISHED"}</StatusBadge></td><td className="text-right"><button className="button button-secondary button-sm">Review</button></td></tr>)}</tbody></table></div></>}
