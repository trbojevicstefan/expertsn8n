import { Plus } from "lucide-react";import { StatusBadge } from "@/components/status-badge";import { showcases } from "@/lib/demo-data";export default function Showcases(){return <><div className="portal-head"><div><h1>Workflow showcases</h1><p>Show the business problem, architecture and outcome — not client secrets.</p></div><button className="button button-primary"><Plus size={16}/>New showcase</button></div><div className="showcase-grid">{showcases.slice(0,3).map(s=><article className="showcase-card card" key={s.id}><StatusBadge tone="success">APPROVED</StatusBadge><h3>{s.title}</h3><p>{s.summary}</p><span className="outcome">{s.outcome}</span></article>)}</div></>}
