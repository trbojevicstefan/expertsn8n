import { requireSession } from "@/lib/auth/server";
export default async function SettingsPage(){const session=await requireSession();return <><div className="portal-head"><div><h1>Settings</h1><p>Account security, notifications and marketplace preferences.</p></div></div><div className="form-card card"><div className="form-section"><h2>Account</h2><div className="field"><label>Email</label><input className="input" value={session.email} readOnly/></div></div><div className="form-section"><h2>Security</h2><p>Firebase Authentication owns sign-in sessions. Enable MFA for privileged admin accounts before production launch.</p></div><div className="form-section"><h2>Notifications</h2><label className="check"><input type="checkbox" defaultChecked/> Contract and milestone updates</label><label className="check"><input type="checkbox" defaultChecked/> Proposals and invitations</label><label className="check"><input type="checkbox" defaultChecked/> Trust & safety notices</label></div></div></>}
