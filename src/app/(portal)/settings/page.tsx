import { requireSession } from "@/lib/auth/server";
export default async function SettingsPage(){const session=await requireSession();return <><div className="portal-head"><div><h1>Settings</h1><p>Account security, notifications and marketplace preferences.</p></div></div><div className="form-card card"><div className="form-section"><h2>Account</h2><div className="field"><label>Email</label><input className="input" value={session.email} readOnly/></div></div><div className="form-section"><h2>Security</h2><p>Signed-in sessions expire on their own and can be ended by logging out. To change your password, use the reset link on the login page.</p><a className="button button-secondary" href="/forgot-password">Change your password</a></div><div className="form-section"><h2>Notifications</h2><label className="check"><input type="checkbox" defaultChecked/> Contract and milestone updates</label><label className="check"><input type="checkbox" defaultChecked/> Proposals and invitations</label><label className="check"><input type="checkbox" defaultChecked/> Trust & safety notices</label></div></div></>}
