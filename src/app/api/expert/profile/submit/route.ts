import { NextResponse } from "next/server";import { z } from "zod";import { getSession } from "@/lib/auth/server";import { adminDb, firebaseAdminConfigured } from "@/lib/firebase/admin";
const schema=z.object({headline:z.string().min(10).max(120),bio:z.string().min(80).max(4000),location:z.string().min(2),timezone:z.string().min(2),rate:z.coerce.number().min(20).max(500),skills:z.array(z.string()).min(2),photoPath:z.string(),cvPath:z.string()});
export async function POST(req:Request){const session=await getSession();if(!session||session.role!=="expert")return NextResponse.json({error:"Expert account required."},{status:401});try{const input=schema.parse(await req.json());const photoPrefix=`private/experts/${session.uid}/photo/`;const cvPrefix=`private/experts/${session.uid}/cv/`;if(!input.photoPath.startsWith(photoPrefix)||!input.cvPath.startsWith(cvPrefix))throw new Error("Profile photo and CV uploads must belong to the signed-in expert.");if(!firebaseAdminConfigured)throw new Error("Firebase Admin is not configured.");const batch=adminDb().batch();batch.set(adminDb().collection("expertProfiles").doc(session.uid),{headline:input.headline,bio:input.bio,location:input.location,timezone:input.timezone,hourlyRate:input.rate,skills:input.skills,status:"SUBMITTED",verified:false,updatedAt:new Date().toISOString()},{merge:true});batch.set(adminDb().collection("expertPrivate").doc(session.uid),{cvStoragePath:input.cvPath,photoStoragePath:input.photoPath},{merge:true});batch.set(adminDb().collection("expertVerifications").doc(session.uid),{state:"SUBMITTED",submittedAt:new Date().toISOString()},{merge:true});await batch.commit();return NextResponse.json({ok:true,state:"SUBMITTED"});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid profile"},{status:400})}}
