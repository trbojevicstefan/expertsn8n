import { NextResponse } from "next/server";import { z } from "zod";import { getSession } from "@/lib/auth/server";import { adminDb, firebaseAdminConfigured } from "@/lib/firebase/admin";import { assertNoOffPlatformContact } from "@/lib/contact-guard";
const schema=z.object({title:z.string().min(8).max(120),description:z.string().min(40).max(8000),visibility:z.enum(["PUBLIC","PRIVATE"]),budgetMin:z.coerce.number().min(100),budgetMax:z.coerce.number().min(100),delivery:z.string().min(2).max(80)}).refine(x=>x.budgetMax>=x.budgetMin,{message:"Maximum budget must be at least the minimum."});
export async function POST(req:Request){const session=await getSession();if(!session||session.role!=="client")return NextResponse.json({error:"Client authentication required."},{status:401});try{const input=schema.parse(await req.json());assertNoOffPlatformContact(input.description);if(!firebaseAdminConfigured)return NextResponse.json({error:"Firebase Admin is not configured."},{status:503});const ref=adminDb().collection("jobs").doc();await ref.set({...input,clientId:session.uid,clientName:session.name||session.email,status:"DRAFT",currency:"EUR",proposalCount:0,createdAt:new Date().toISOString()});return NextResponse.json({id:ref.id,status:"DRAFT"},{status:201});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid job"},{status:400})}}
