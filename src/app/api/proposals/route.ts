import { NextResponse } from "next/server";import { z } from "zod";import { getSession } from "@/lib/auth/server";import { adminDb, firebaseAdminConfigured } from "@/lib/firebase/admin";import { assertNoOffPlatformContact } from "@/lib/contact-guard";
const schema=z.object({jobId:z.string().min(3),scope:z.string().min(60).max(6000),price:z.number().min(100),deliveryDays:z.number().int().min(1).max(365)});
export async function POST(req:Request){const session=await getSession();if(!session||session.role!=="expert")return NextResponse.json({error:"Expert account required."},{status:401});try{const input=schema.parse(await req.json());assertNoOffPlatformContact(input.scope);if(!firebaseAdminConfigured)throw new Error("Firebase Admin is not configured.");const ref=adminDb().collection("proposals").doc();await ref.set({...input,expertId:session.uid,status:"SUBMITTED",currency:"EUR",createdAt:new Date().toISOString()});return NextResponse.json({id:ref.id},{status:201});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid proposal"},{status:400})}}
