import { notFound } from "next/navigation";import Link from "next/link";import { BadgeCheck, Clock3, LockKeyhole } from "lucide-react";import { SiteHeader } from "@/components/site-header";import { Footer } from "@/components/footer";import { findJob } from "@/lib/data";
export const dynamic = "force-dynamic";
export default async function JobPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const job=await findJob(id);if(!job)notFound();return <><SiteHeader/><main><div className="container job-detail-grid"><article className="detail-card card"><span className="eyebrow">Posted {job.postedAt}</span><h1>{job.title}</h1><div className="job-footer" style={{borderTop:0,paddingTop:0,marginTop:0,marginBottom:23}}><span><BadgeCheck size={15}/>Payment verified</span><span><Clock3 size={15}/>{job.delivery}</span><span>{job.proposalCount} proposals</span></div><h3>What we need</h3><p>{job.description}</p><h3>Skills & integrations</h3><div className="chip-row">{[...job.skills,...job.integrations].map(x=><span className="chip" key={x}>{x}</span>)}</div><div className="notice" style={{marginTop:26}}><strong>Platform communication rule</strong>Direct contact details, phone numbers, email addresses and external meeting links are blocked before the first milestone is funded. Use proposal fields for scope clarification.</div></article><aside className="job-sidebar card"><span className="muted">Fixed-price budget</span><div className="money-big">€{job.budgetMin.toLocaleString()}–€{job.budgetMax.toLocaleString()}</div><p className="muted">Expected delivery: {job.delivery}</p><Link className="button button-primary button-wide" href={`/sign-in?next=/jobs/${job.id}`}>Submit a proposal</Link><div className="verified-box"><LockKeyhole size={18}/><div><strong>Funds-first workflow</strong><br/>Full communication unlocks when a contract milestone is funded.</div></div></aside></div></main><Footer/></>}
