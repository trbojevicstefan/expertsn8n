import { SiteHeader } from "@/components/site-header";import { Footer } from "@/components/footer";import { JobCard } from "@/components/job-card";import { listPublicJobs } from "@/lib/data";
export const metadata={title:"n8n jobs",description:"Browse n8n automation projects from payment-ready clients."};
export default async function Jobs(){const jobs=await listPublicJobs();return <><SiteHeader/><main><section className="page-hero"><div className="container"><span className="eyebrow">Open work</span><h1>High-signal n8n projects.</h1><p>Public jobs from clients looking specifically for n8n and automation expertise. Verified experts can submit proposals through the platform.</p></div></section><div className="container directory-layout"><aside className="filters"><div className="filter-block"><h4>Budget</h4>{["Under €2,000","€2,000–€5,000","€5,000+"].map(x=><label className="check" key={x}><input type="checkbox"/>{x}</label>)}</div><div className="filter-block"><h4>Skills</h4>{["AI agents","CRM","APIs","Data","Observability"].map(x=><label className="check" key={x}><input type="checkbox"/>{x}</label>)}</div></aside><section><div className="results-toolbar"><span><strong>{jobs.length}</strong> open projects</span><select className="select" style={{width:180}}><option>Newest first</option><option>Highest budget</option></select></div><div className="job-list">{jobs.map(j=><JobCard job={j} key={j.id}/>)}</div></section></div></main><Footer/></>}
