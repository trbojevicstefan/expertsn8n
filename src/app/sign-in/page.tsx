import { Suspense } from "react";import { CheckCircle2 } from "lucide-react";import { SiteHeader } from "@/components/site-header";import { AuthForm } from "@/components/auth-form";
export const metadata={title:"Log in"};export default function SignIn(){return <><SiteHeader/><main className="auth-wrap"><section className="auth-art"><h2>One place for the whole automation engagement.</h2><p>From expert discovery to funded milestones, delivery, review and dispute evidence.</p><div className="auth-proof"><span><CheckCircle2 size={18}/>Reviewed expert profiles</span><span><CheckCircle2 size={18}/>Public and private jobs</span><span><CheckCircle2 size={18}/>Payment-gated collaboration</span></div></section><section className="auth-form-wrap"><Suspense fallback={null}><AuthForm mode="sign-in"/></Suspense></section></main></>}
